-- A Single-line comment is one that starts with wo dashes
--CREATE DATABASE MyDatabes
--GO  --Used to indicate that the above code should run as "batch"
--DROP DATABASE MyDatabes
--GO

--[ctrl] + r - toggles messages/results window
--{ctrl] + k then [ctrl] + c to comment out multiple lines
USE MyDatabse -- change the execution context for the script that follows
GO--run as batch 